package src

/*
r4, err := c.SendMetrics(ctx, &pb.SendMetricsRequest{Metrics: map[string]string{"key": "value"}})
	if err != nil {
		log.Fatalf("Не удалось отправить метрики: %v", err)
	}
	log.Printf("Status: %s", r4.GetStatus())


	r5, err := c.LogMetrics(ctx, &pb.LogMetricsRequest{Metrics: map[string]string{"key": "value"}})
	if err != nil {
		log.Fatalf("Не удалось записать метрики: %v", err)
	}
	log.Printf("Status: %s", r5.GetStatus())
}
*/
